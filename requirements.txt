# Pinned to the exact versions the app is tested against, so a dependency
# update can't silently break it. Bump deliberately, then run the tests.
PyQt6==6.11.0
yfinance==1.4.1
mplfinance==0.12.10b0
matplotlib==3.10.9
pandas==3.0.3
anthropic==0.105.2

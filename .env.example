# Copy this file to ".env" (same folder) and put your real key in.
# The .env file is gitignored, so your key won't be committed.
# No quotes, no spaces around the = sign.
ANTHROPIC_API_KEY=sk-ant-your-key-here
